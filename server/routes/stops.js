const express = require('express');
const router = express.Router();

// Placeholder routes - will be implemented next
router.get('/', (req, res) => {
  res.json({ message: 'Stops endpoint - Coming soon' });
});

module.exports = router;