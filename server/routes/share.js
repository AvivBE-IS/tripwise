const express = require('express');
const router = express.Router();

// Placeholder routes - will be implemented next
router.get('/:token', (req, res) => {
  res.json({ message: 'Share endpoint - Coming soon' });
});

module.exports = router;