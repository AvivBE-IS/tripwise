# Route-
Travel&amp;Joy is an AI trip planner. Type a prompt; get a visual itinerary with coords, images, map + timeline. Edit stops and custom routes. Packing lists adapt to weather and trip type. Track flights via Aviationstack. Share as link/PDF/ICS. Built with React + Node + Postgres; JWT auth; user/admin. OpenAI JSON schema guards for reliable output.
